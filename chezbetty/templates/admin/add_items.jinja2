{% extends "base.jinja2" %}
{% set active_page = 'add_items' %}
{% block title %}Add Items{% endblock %}

{% block top %}
<h1 class="page-header">Add Items</h1>
{% endblock %}


{% block content %}

<form role="form" action="/admin/add/items/submit" method="post">

  <div class="row item-list-header">
    <div class="col-sm-3">Name</div>
    <div class="col-sm-2">Barcode</div>
    <div class="col-sm-2">In Stock</div>
    <div class="col-sm-2">Item Price</div>
    <div class="col-sm-2">Item Wholesale</div>
    <div class="col-sm-1">Enabled</div>
  </div>

  <div id="new-items">
    {% for item in items %}
    <div id="new-item-0" class="form-group row new-item">
      <div class="col-sm-3"><input type="text" class="form-control" id="item-name-{{ loop.index }}" name="item-name-{{ loop.index }}" value={{ item['name'] }}></div>
      <div class="col-sm-2"><input type="text" class="form-control" id="item-barcode-{{ loop.index }}" name="item-barcode-{{ loop.index }}" value={{ item['barcode'] }}></div>
      <div class="col-sm-2"><input type="text" class="form-control" id="item-stock-{{ loop.index }}" name="item-stock-{{ loop.index }}" value={{ item['in_stock'] }}></div>
      <div class="col-sm-2"><input type="text" class="form-control" id="item-price-{{ loop.index }}" name="item-price-{{ loop.index }}" value={{ item['price'] }}></div>
      <div class="col-sm-2"><input type="text" class="form-control" id="item-wholesale-{{ loop.index }}" name="item-wholesale-{{ loop.index }}" value={{ item['wholesale'] }}></div>
      <div class="col-sm-1"><input type="checkbox" id="item-enabled-{{ loop.index }}" name="item-enabled-{{ loop.index }}" {% if item['enabled'] %}checked="checked"{% endif %}></div>
    </div>
    {% endfor %}
  </div>

  <button type="button" id="btn-items-add-row" class="btn btn-success">Add Another Item</button>
  <button type="submit" class="btn btn-success">Add Items</button>

  <input type="hidden" id="new-items-number" name="new-items-number" value="{{ items|length }}">

</form>

{% endblock %}

{% block onload %}
<script src="{{'chezbetty:static/js/chezbetty-admin-onload.js'|static_url}}"></script>
{% endblock %}